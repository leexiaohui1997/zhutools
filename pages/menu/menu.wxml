<navigation-container title="便捷操作" noTop>
  <group
    wx:for="{{ group }}"
    wx:key="title"
    title="{{ item.title }}"
    tools="{{ item.tools }}"
    empty-text="{{ item.empty }}"
  ></group>
</navigation-container>
